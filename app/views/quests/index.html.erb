<div class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 py-8 px-4">
  <div class="max-w-4xl mx-auto">
    <!-- Header -->
    <div class="text-center mb-8">
      <h1 class="text-4xl md:text-5xl font-bold text-blue-400 mb-2">My Brag Quest</h1>
      
    </div>
  <!-- Profile Section -->
    <div class="text-center mb-8">
      <div class="relative inline-block mb-6">
        <%= image_tag "Profile.jpg", class: "w-24 h-24 md:w-64 md:h- rounded-full border-4 border-white shadow-2xl object-cover mx-auto" %>
        <p class="text-gray-600 pt-8 text-2xl md:text-3xl">Wanus Kurirung (สุดหล่อ)</p>
        <div class="mt-2">
          <%= link_to 'My Brag', brag_index_path, class: "inline-block bg-indigo-500 hover:bg-indigo-600 text-white font-semibold py-2 px-4 rounded-lg transition duration-200" %>
      </div>
      </div>
    </div>
    <!-- New Quest Form Card -->
    <div class="bg-white rounded-2xl shadow-lg p-6 mb-8 border border-gray-200">
      <h2 class="text-xl font-semibold text-gray-800 mb-4 flex items-center">
        <span class="mr-2">✨</span>
        Add New Quest
      </h2>
      <%= turbo_frame_tag "new_quest" do %>
        <%= render 'form', quest: Quest.new %>
      <% end %>
    </div>

    <!-- Quests List -->
    <div class="bg-white rounded-2xl shadow-lg border border-gray-200 overflow-hidden">
      <div class="p-6 border-b border-gray-200">
        <h2 class="text-xl font-semibold text-gray-800 flex items-center">
          <span class="mr-2">📋</span>
          Your Quests
        </h2>
      </div>
      
      <ul id="quests" class="divide-y divide-gray-100">
        <% @quests.each do |quest| %>
          <%= render 'quest', quest: quest %>
        <% end %>
      </ul>
    </div>
  </div>
</div>